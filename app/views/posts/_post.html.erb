 <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= post.content %>
  </p>
  <% post.comments.each do |comment| %>
    <%= comment.user.email %> : </br>
    <%= comment.description %> : </br>
  </br>
  <% end %>
</div>
