<div id="<%= dom_id user %>">
  <% if user %>
    <p>
      <strong>Email:</strong>
      <%= user.email %>
    </p>

    <p>
      <strong>Password:</strong>
      <%= user.password %>
    </p>
  <% end %>

</div>
